# Database (configured in Stage 2)
DATABASE_URL=
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Authentication (configured in Stage 3)
AUTH_SECRET=
AUTH_URL=

# Rate limiting (optional; Upstash Redis REST). When unset, rate limiting is disabled.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
